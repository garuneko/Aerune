Place the audited macOS arm64 LGPL FFmpeg executable at:

    vendor/ffmpeg/darwin-arm64/ffmpeg

Build it with:

    scripts/build-ffmpeg-darwin-arm64.sh

The build script also writes build configuration, checksum, and license notice files.
